{
  "name": "division-project",
  "version": "4.0.0",
  "scripts": {
    "start": "cd division-project && npm install && npm start",
    "build": "cd division-project && npm install && npm start"
  },
  "dependencies": {
    "babel-preset-es2015": "^6.24.0"
  }
}
